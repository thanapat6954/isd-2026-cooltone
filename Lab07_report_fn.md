# 6.6 สิ่งที่ต้องบันทึกจากส่วนนี้ (Summary & Experimental Logs)[cite: 6]

ตารางบันทึกผลการทดลองสกัดข้อมูลแผนการศึกษาจากเอกสาร PDF (`DSBA.pdf`) ผ่าน Pipeline VLM (`scb10x/typhoon-ocr1.5-3b` + LLM) โดยทำการเปรียบเทียบผลลัพธ์จากการปรับเปลี่ยนตัวแปร 3 ส่วนหลัก ได้แก่ ขนาดก้อน (Chunk Size), ความละเอียดภาพ (Image DPI) และขนาดโมเดลข้อความ (Text LLM Model Size)[cite: 6]

---

### 1. ผลการทดลองปรับขนาดก้อน (Chunk Size)
ประมวลผลเอกสารจำนวน 7 หน้า (หน้า 30–36)[cite: 6]

| ตัวแปร (Chunk Size) | จำนวนวิชาที่พบ (Recall / GT) | Precision (P) | Recall (R) | F1-Score | Micro CER | เวลาที่ใช้รวม (วินาที) |
| :--- | :---: | :---: | :---: | :---: | :---: | :---: |
| **LAB7_CHUNK = 1** | 13 / 90 | 0.591 | 0.144 | 0.232 | 0.9350 | 1,701.8 |
| **LAB7_CHUNK = 3** | 0 / 90 | 0.000 | 0.000 | 0.000 | 1.0000 | 1,312.5 |
| **LAB7_CHUNK = 6** | 6 / 90 | 0.667 | 0.067 | 0.121 | 0.9697 | 1,351.1 |

* **วิเคราะห์ผลลัพธ์:**
  * **Chunk = 1** ให้ประสิทธิภาพสูงสุด (F1-Score = 0.232, Recall = 0.144) เนื่องจากเป็นการส่งภาพแยกทีละหน้า ทำให้ Context Window ไม่แออัด และ OCR/LLM สามารถสกัดวิชาได้ถูกต้องรวม 13 รายการ[cite: 6]
  * **Chunk = 3** ล้มเหลวโดยสิ้นเชิง (Recall = 0.000) เกิดปัญหา Token Truncation จากโมเดล OCR และ LLM สร้างข้อมูลผิดโครงสร้าง (เช่น รหัสวิชาหลุดเป็น `MATH101`, `PHYS101`)[cite: 6]
  * **Chunk = 6** ประสิทธิภาพลดลงกว่า Chunk = 1 (F1-Score = 0.121) เนื่องจากข้อความ OCR ชนขีดจำกัด `num_predict` (16,384 tokens) ในหลายหน้า ส่งผลให้ข้อมูลบางส่วนถูกตัดทอน[cite: 6]

---

### 2. ผลการทดลองปรับความละเอียดภาพ (Image DPI)
ประมวลผลเอกสารจำนวน 1 หน้า (หน้า 30)[cite: 6]

| ความละเอียด (DPI) | จำนวนวิชาที่พบ | Precision (P) | Recall (R) | F1-Score | Micro CER | เวลา OCR (วิ) | เวลา LLM (วิ) | เวลาที่ใช้รวม (วิ) |
| :--- | :---: | :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| **LAB7_DPI = 100** | 7 / 90 | 1.000 | 0.078 | 0.144 | 0.9692 | 13.5 | 172.8 | 186.6 |
| **LAB7_DPI = 150** | 7 / 90 | 1.000 | 0.078 | 0.144 | 0.9653 | 15.3 | 142.0 | 157.6 |
| **LAB7_DPI = 300** | 0 / 90 | 0.000 | 0.000 | 0.000 | 1.0000 | 274.6 | 75.7 | ~350.3 |

* **วิเคราะห์ผลลัพธ์:**
  * **150 DPI** เป็นความละเอียดที่เหมาะสมที่สุด ให้เวลาประมวลผลรวมเร็วที่สุด (157.6 วินาที) และมีค่า Micro CER ดีที่สุด (0.9653)[cite: 6]
  * **100 DPI** ทำงานได้แม่นยำเท่า 150 DPI (สกัดได้ 7 วิชา, Precision = 1.000) แต่อาจมีข้อผิดพลาดระดับตัวอักษรเล็กน้อย ทำให้ CER สูงขึ้นกว่าเล็กน้อย (0.9692)[cite: 6]
  * **300 DPI** ล้มเหลว เนื่องจากขนาดภาพใหญ่เกินไปส่งผลให้ Typhoon-OCR สร้าง Token เกินขีดจำกัดสูงสุด (`num_predict = 16,384 tokens`) ทำให้ข้อความถูกตัดทอนและ LLM สกัด JSON ไม่สำเร็จ[cite: 6]

---

### 3. ผลการทดลองปรับขนาดโมเดลข้อความ (Text LLM Model Size)
ประมวลผลเอกสารจำนวน 1 หน้า (หน้า 30, DPI = 150)[cite: 6]

| ขนาดโมเดล LLM | จำนวนวิชาที่พบ | Precision (P) | Recall (R) | F1-Score | Micro CER | เวลา LLM (วิ) | เวลาที่ใช้รวม (วิ) |
| :--- | :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| **qwen3:1.7b** | 4 / 90 | 0.571 | 0.044 | 0.083 | 0.9791 | 83.0 | 113.9 |
| **qwen3:4b** *(Baseline)* | 7 / 90 | 1.000 | 0.078 | 0.144 | 0.9653 | 142.0 | 157.6 |

* **วิเคราะห์ผลลัพธ์:**
  * **qwen3:1.7b** ใช้เวลาประมวลผลส่วน LLM เร็วกว่า (83.0 วินาที) แต่ความถูกต้องลดลงอย่างมาก (Precision = 0.571, F1 = 0.083) มีอาการสร้างข้อมูลสุ่ม (Hallucination) และจัดรูปแบบเงื่อนไข JSON ผิดพลาด (เช่น กำหนด `year_semester = 0` โดยไม่ระบุ `flexible_year_semester`)[cite: 6]
  * **qwen3:4b** มีความเข้าใจโครงสร้างและเงื่อนไขของ JSON schema ดีกว่า ให้ค่า Precision 1.000 (ไม่มีการแต่งข้อมูลเกิน) แม้จะใช้เวลาประมวลผลนานกว่าเล็กน้อย[cite: 6]

---

### 4. ตัวอย่างไฟล์กลางทาง (Intermediate VLM Output)

ตัวอย่างผลลัพธ์ข้อความดิบ (Raw Text Output) ที่สกัดได้จากโมเดล OCR (`scb10x/typhoon-ocr1.5-3b`) ในไฟล์ `intermediate_vlm.md`:

```markdown
<!-- ตัวอย่างข้อความตารางที่ OCR อ่านได้จากหน้า 30 -->
| รหัสวิชา | ชื่อวิชา | จำนวนหน่วยกิต |
| :--- | :--- | :--- |
| DSBA101 | Intro to Data Science and Business Analytics | 3(3-0-6) |
| MATH105 | Calculus for Business I | 3(3-0-6) |

---

### 5. ข้อสรุปการเลือก Pipeline (Pipeline Selection Conclusion)

จากการทดลองเปรียบเทียบตัวแปรต่าง ๆ ในระบบ Pipeline สามารถสรุปการเลือกตั้งค่าที่เหมาะสมที่สุดสำหรับเอกสาร `DSBA.pdf` โดยอ้างอิงจากตัวเลขผลการทดลองเชิงสถิติ (ไม่ใช้ความรู้สึก):

* **Pipeline ที่เหมาะสมที่สุด:** `Pipeline VLM (Typhoon-OCR 1.5-3B + Qwen3:4B)` ที่ตั้งค่า **`Chunk Size = 1`** และ **`DPI = 150`**
* **เหตุผลสนับสนุนด้วยตัวเลขเชิงปริมาณ:**
  * **ความแม่นยำของข้อมูล (Precision = 1.000):** การใช้โมเดล `qwen3:4b` ให้ค่า Precision 100% (1.000) ซึ่งสูงกว่า `qwen3:1.7b` (Precision = 0.571) ถึง **42.9%** แสดงให้เห็นว่าโมเดลขนาดใหญ่กว่าสกัดข้อมูลได้ถูกต้องโดยไม่สร้างข้อมูลเท็จ (Hallucination)
  * **ประสิทธิภาพการดึงข้อมูลวิชา (F1-Score = 0.232):** การตั้งค่า `Chunk Size = 1` ให้ F1-Score สูงที่สุด (0.232) และสกัดวิชาได้มากสุด 13 วิชา ในขณะที่ `Chunk Size = 3` ได้ F1-Score **0.000** (ล้มเหลว 100% จาก Token Truncation)
  * **อัตราความผิดพลาดอักขระต่ำสุด (Micro CER = 0.9653):** การตั้งค่าความละเอียดที่ **150 DPI** ให้ค่า CER ต่ำที่สุด ดีกว่า 100 DPI (0.9692) และ 300 DPI ที่ได้ค่า CER **1.0000** (อ่านข้อมูลไม่ได้เนื่องจากภาพใหญ่เกินไป)
  * **ความคุ้มค่าด้านเวลา (Total Time = 157.6s):** ความละเอียด 150 DPI ใช้เวลาประมวลผลรวม 157.6 วินาที เร็วกว่า 300 DPI (ใช้เวลา >350.3 วินาที) ถึง **55%** โดยที่ยังรักษาความแม่นยำของข้อมูลไว้ได้สูงสุด

---

### 6. สรุปข้อแนะนำสำหรับการกำหนดค่าระบบ (Recommendations)

1. **Chunk Size:** ควรตั้งค่า `LAB7_CHUNK = 1` เพื่อป้องกันปัญหาข้อความยาวเกินไปจนเกิด Token Truncation[cite: 6]
2. **Image DPI:** ควรใช้ความละเอียด `LAB7_DPI = 150` เพื่อความสมดุลระหว่างความคมชัดของข้อความและความเร็วในการประมวลผล[cite: 6]
3. **Text LLM Model:** ควรใช้โมเดลขนาด `4b` ขึ้นไป (เช่น `qwen3:4b`) เพื่อการสกัดข้อมูลลงโครงสร้าง JSON ที่แม่นยำและถูกต้องตาม Schema[cite: 6]